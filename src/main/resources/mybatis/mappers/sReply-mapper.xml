<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SReplyMapper">
	
	<insert id="insertReply">
		INSERT INTO S_REPLY_TBL 
		VALUES(S_SEQ_NO.NEXTVAL, #{sNo}, #{uId}, #{sRContent}, DEFAULT, DEFAULT, DEFAULT)
	</insert>
	
	<update id="updateReply">
		UPDATE S_REPLY_TBL 
		SET S_R_CONTENT = #{sRContent}, S_R_UPDATE = DEFAULT WHERE S_R_NO = #{sRNo} 
	</update>
	
	<update id="deleteReply">
		UPDATE S_REPLY_TBL 
		SET S_R_UPDATE = DEFAULT, S_R_STATUS = 'N' WHERE S_R_NO = #{sRNo} 
	</update>
	
	
	<select id="getTotalCount" resultType="_int">
		SELECT COUNT(*) FROM S_REPLY_TBL 
		WHERE S_NO = #{sNo} AND S_R_STATUS = 'Y'
	</select>
	<select id="selectSReplyList" resultType="SupportReply">
		SELECT * FROM S_REPLY_TBL 
		WHERE S_NO = #{sNo} AND S_R_STATUS = 'Y' ORDER BY S_R_NO DESC
	</select>
	

</mapper>