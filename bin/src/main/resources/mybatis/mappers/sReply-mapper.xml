<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SReplyMapper">
	<resultMap type="SupportReply" id="SReplyResultMap">
		<id 	property="sRNo" 	 column="S_H_NO"/>
		<result property="sNo" 	 	 column="S_NO"/>
		<result property="uId" 	  	 column="U_ID"/>
		<result property="sRContent" column="S_H_NAME"/>
		<result property="sRCreate"	 column="S_H_AMOUNT"/>
		<result property="sRUpdate"  column="S_H_PAYDATE"/>
		<result property="sRStatus"  column="S_H_PAYTYPE"/>
	</resultMap>
	
	<insert id="insertSReply">
		INSERT INTO S_REPLY_TBL VALUES(S_SEQ_NO.NEXTVAL, #{sNo}, #{uId}, #{sRContent}, DEFAULT, DEFAULT, DEFAULT)
	</insert>
	
	<update id="updateSReply">
		UPDATE S_REPLY_TBL SET S_R_CONTENT = #{sRContent}, S_R_UPDATE = DEFAULT WHERE S_R_NO = #{sRNo} 
	</update>
	
	<update id="deleteSReply">
		UPDATE S_REPLY_TBL SET S_R_UPDATE = DEFAULT, S_R_STATUS = 'N' WHERE S_R_NO = #{sRNo} 
	</update>
	
	
	<select id="getTotalCount" resultType="_int">
		SELECT COUNT(*) FROM S_REPLY_TBL WHERE S_NO = #{sNo} AND S_R_STATUS = 'Y'
	</select>
	<select id="selectSReplyList" resultMap="SReplyResultMap">
		SELECT * FROM S_REPLY_TBL WHERE S_NO = #{sNo} AND S_R_STATUS = 'Y' ORDER BY S_R_NO DESC
	</select>
	

</mapper>